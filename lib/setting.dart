import 'package:flutter/widgets.dart';

class SettingeNav extends StatelessWidget {
  const SettingeNav({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('الاعدادات');
  }
}
