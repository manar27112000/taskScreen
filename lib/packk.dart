import 'package:flutter/widgets.dart';

class PacNav extends StatelessWidget {
  const PacNav({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('الباقات');
  }
}
